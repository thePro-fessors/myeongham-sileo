import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.online.businesscard',
  appName: 'Online Business Card',
  webDir: 'out'
};

export default config;
